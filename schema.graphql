type VestingSchedule @entity {
  id: ID!
  vestingScheduleId: Bytes!,
  beneficiary: Bytes!,
  cliff: Int!,
  start: Int!,
  duration: Int!,
  slicePeriodSeconds: Int!,
  revocable: Boolean!,
  amountTotal: BigInt!,
  released: BigInt!,
  revoked: Boolean!,
  upFront: BigInt!
}